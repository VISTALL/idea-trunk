//test for inspection CyclomaticComplexity
function foo()
{
    function bar()
    {
        if(true)
          bar;
        if(true)
          bar;
    }
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    if(true)
      a = b;
    while(true);
}

